# Model checkpoint

Place the pretrained checkpoint at:

```text
model/checkpoint-step0240000.pth
```

Files such as `*.pth`, `*.pt`, `*.ckpt`, and `*.safetensors` are excluded from
Git by the repository `.gitignore`.
